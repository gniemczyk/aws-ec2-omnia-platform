# =============================================================================
# Grafana - zmienne i konfiguracja (zmienione na przekazywane przez Ansible)
# =============================================================================
# Operacje SSM zostały przeniesione do GitHub Actions, aby uniknąć problemów
# z niestabilnością połączenia przez SSM podczas wdrażania.
# =============================================================================
