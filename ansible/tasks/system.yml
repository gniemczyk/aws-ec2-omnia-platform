# =============================================================================
# FAZA 1: Przygotowanie systemu
# =============================================================================

- name: Aktualizacja cache apt
  apt:
    update_cache: true
    cache_valid_time: 3600

- name: Instalacja wymaganych pakietow systemowych
  apt:
    name:
      - docker.io
      - docker-compose
      - jq
      - curl
      - ca-certificates
    state: present

- name: Uruchomienie i wlaczenie uslugi Docker
  systemd:
    name: docker
    state: started
    enabled: true

- name: Upewnienie sie, ze grupa docker istnieje
  group:
    name: docker
    state: present

- name: Dodanie ssm-user do grupy docker
  user:
    name: ssm-user
    groups: docker
    append: yes
